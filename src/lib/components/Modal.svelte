<script lang="ts">
  import type { Snippet } from "svelte";

  let {
    children,
    oncancel,
  }: {
    children: Snippet;
    oncancel: () => void;
  } = $props();
</script>

<div class="modal-backdrop" role="presentation" onclick={oncancel}>
  <!-- svelte-ignore a11y_click_events_have_key_events -->
  <!-- svelte-ignore a11y_no_static_element_interactions -->
  <div class="modal" onclick={(e) => e.stopPropagation()}>
    {@render children()}
  </div>
</div>

<style>
  .modal-backdrop {
    position: fixed;
    inset: 0;
    background: rgba(0, 0, 0, 0.4);
    display: flex;
    align-items: center;
    justify-content: center;
    z-index: 100;
  }

  .modal {
    background: #fff;
    border-radius: 8px;
    padding: 24px;
    min-width: 360px;
    max-width: 520px;
    box-shadow: 0 8px 32px rgba(0, 0, 0, 0.2);
  }

  .modal :global(h3) {
    margin: 0 0 8px;
    font-size: 16px;
  }

  .modal :global(p) {
    margin: 0 0 16px;
    font-size: 13px;
    color: #555;
  }

  .modal :global(.modal-actions) {
    display: flex;
    justify-content: flex-end;
    gap: 8px;
    margin-top: 16px;
  }

  .modal :global(.field) {
    display: block;
    margin-bottom: 12px;
  }

  .modal :global(.field-label) {
    display: block;
    font-size: 12px;
    font-weight: 600;
    margin-bottom: 4px;
    color: #555;
  }

  .modal :global(.field input) {
    width: 100%;
    padding: 8px 10px;
    border: 1px solid #ccc;
    border-radius: 4px;
    font: inherit;
    font-size: 13px;
  }

  .modal :global(.field textarea) {
    width: 100%;
    padding: 8px 10px;
    border: 1px solid #ccc;
    border-radius: 4px;
    font: inherit;
    font-size: 13px;
    resize: vertical;
  }

  .modal :global(.field select) {
    width: 100%;
    padding: 8px 10px;
    border: 1px solid #ccc;
    border-radius: 4px;
    font: inherit;
    font-size: 13px;
  }

  .modal :global(.field input:focus) {
    outline: none;
    border-color: #1a1a2e;
    box-shadow: 0 0 0 2px rgba(26, 26, 46, 0.15);
  }

  .modal :global(.field textarea:focus) {
    outline: none;
    border-color: #1a1a2e;
    box-shadow: 0 0 0 2px rgba(26, 26, 46, 0.15);
  }

  .modal :global(.field select:focus) {
    outline: none;
    border-color: #1a1a2e;
    box-shadow: 0 0 0 2px rgba(26, 26, 46, 0.15);
  }
</style>
